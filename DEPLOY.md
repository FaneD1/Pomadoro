# 🚀 Инструкция по деплою Pomodoro Pair

## Вариант 1: Railway (Рекомендуется - самый простой)

### Шаги:

1. **Зарегистрируйтесь на Railway:**
   - Перейдите на [railway.app](https://railway.app)
   - Войдите через GitHub

2. **Создайте новый проект:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите репозиторий `FaneD1/Pomadoro`

3. **Настройте деплой:**
   - Railway автоматически определит Node.js проект
   - Убедитесь, что команда запуска: `npm start`
   - Railway автоматически установит переменную `PORT`

4. **Готово!**
   - Railway предоставит URL вида: `https://your-app.railway.app`
   - Приложение будет доступно в интернете

### Преимущества Railway:
- ✅ Бесплатный tier с $5 кредитами в месяц
- ✅ Автоматический HTTPS
- ✅ Поддержка WebSocket из коробки
- ✅ Простой деплой через GitHub

---

## Вариант 2: Render (Бесплатный)

### Шаги:

1. **Зарегистрируйтесь на Render:**
   - Перейдите на [render.com](https://render.com)
   - Войдите через GitHub

2. **Создайте новый Web Service:**
   - Нажмите "New +" → "Web Service"
   - Подключите репозиторий `FaneD1/Pomadoro`

3. **Настройки:**
   - **Name:** pomodoro-pair
   - **Environment:** Node
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
   - **Plan:** Free

4. **Переменные окружения:**
   - `NODE_ENV` = `production`
   - `PORT` = `10000` (Render автоматически установит, но можно указать явно)

5. **Готово!**
   - Render предоставит URL: `https://pomodoro-pair.onrender.com`

### Важно для Render:
- ⚠️ Бесплатные инстансы "засыпают" после 15 минут бездействия
- ⚠️ Первый запрос после простоя может занять 30-60 секунд
- ✅ Полностью бесплатно
- ✅ Автоматический HTTPS

---

## Вариант 3: Fly.io

### Шаги:

1. **Установите Fly CLI:**
   ```bash
   # Windows (PowerShell)
   iwr https://fly.io/install.ps1 -useb | iex
   
   # Или через npm
   npm install -g @fly/cli
   ```

2. **Войдите в Fly.io:**
   ```bash
   fly auth login
   ```

3. **Создайте приложение:**
   ```bash
   fly launch
   ```
   - Выберите регион (например, `iad` для США)
   - Не создавайте PostgreSQL (используем SQLite)

4. **Деплой:**
   ```bash
   fly deploy
   ```

5. **Готово!**
   - URL будет: `https://your-app.fly.dev`

### Преимущества Fly.io:
- ✅ Бесплатный tier (3 shared-cpu VMs)
- ✅ Быстрый старт приложений
- ✅ Отличная поддержка WebSocket
- ✅ Глобальная сеть

---

## Вариант 4: Vercel (Только Frontend) + Backend отдельно

⚠️ **Не рекомендуется** для этого проекта, так как Vercel не поддерживает WebSocket на бесплатном плане.

---

## После деплоя

### Проверка работы:

1. Откройте ваш URL в браузере
2. Войдите с invite-кодом
3. Откройте в другой вкладке/браузере
4. Проверьте, что WebSocket работает (должны видеть обновления в реальном времени)

### Возможные проблемы:

**WebSocket не работает:**
- Убедитесь, что платформа поддерживает WebSocket
- Проверьте, что используется `wss://` (не `ws://`) на HTTPS

**База данных:**
- SQLite файл будет создаваться автоматически на сервере
- При перезапуске контейнера данные могут потеряться (для продакшена лучше использовать PostgreSQL)

---

## Рекомендации для продакшена

1. **База данных:**
   - Замените SQLite на PostgreSQL (Railway, Render предоставляют бесплатные БД)
   - Или используйте внешний сервис (Supabase, Neon)

2. **Переменные окружения:**
   - Добавьте `NODE_ENV=production`
   - Настройте CORS, если нужно

3. **Мониторинг:**
   - Добавьте логирование
   - Настройте алерты

---

## Быстрый старт (Railway)

```bash
# 1. Установите Railway CLI (опционально)
npm i -g @railway/cli

# 2. Войдите
railway login

# 3. Создайте проект
railway init

# 4. Деплой
railway up
```

Или просто через веб-интерфейс Railway - это проще! 🚀

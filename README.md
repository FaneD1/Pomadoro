# 🍅 Pomodoro Pair

Веб-приложение Pomodoro для двух пользователей с realtime синхронизацией таймеров и задач.

## 📋 Описание

Pomodoro Pair позволяет двум пользователям работать вместе, видя таймеры и активные задачи друг друга в реальном времени. Каждый пользователь управляет только своим таймером, но видит состояние партнёра.

## 🏗️ Архитектура

### Backend (Node.js + Express)

```
backend/
├── database.js      # Инициализация SQLite и промис-обёртки
├── routes/
│   ├── auth.js      # Аутентификация по invite-коду
│   ├── tasks.js     # CRUD операции для задач
│   ├── timer.js     # Управление таймером (start/pause/stop)
│   └── partner.js   # Получение состояния партнёра
└── websocket.js     # WebSocket сервер для realtime синхронизации
```

### Frontend (Vanilla JavaScript)

```
public/
├── index.html   # Структура приложения
├── styles.css   # Стили и UI
└── app.js       # Логика клиента и WebSocket клиент
```

### База данных (SQLite)

**Таблицы:**
- `rooms` - комнаты для пар пользователей
- `users` - пользователи с invite-кодами
- `tasks` - задачи пользователей
- `timer_sessions` - сессии таймеров

## 🔐 Верификация

### Как работает вход по invite-коду

1. **Первый пользователь:**
   - Вводит уникальный invite-код (например, "alice123")
   - Вводит своё имя
   - Система создаёт нового пользователя и новую комнату
   - Пользователь закрепляется за комнатой

2. **Второй пользователь:**
   - Вводит тот же invite-код, что и первый пользователь
   - Вводит своё имя
   - Система находит комнату с одним пользователем
   - Пользователь присоединяется к этой комнате

3. **Повторный вход:**
   - Если пользователь с таким invite-кодом уже существует, он просто входит
   - Состояние сохраняется (задачи, таймер)

**Особенности:**
- Нет паролей — только уникальный invite-код
- Cookie-based сессии (httpOnly для безопасности)
- Автоматическое создание/присоединение к комнате

## ⏱️ Таймер и задачи

### Связь таймера и задачи

1. **Создание задачи:**
   - Пользователь создаёт задачи через интерфейс
   - Задачи сохраняются в БД с `user_id`

2. **Активация задачи:**
   - Пользователь выбирает одну активную задачу
   - Все остальные задачи деактивируются
   - При запуске таймера он привязывается к активной задаче

3. **Работа таймера:**
   - Таймер хранит `task_id` в сессии
   - Партнёр видит название активной задачи
   - При смене задачи таймер продолжает работать с новой задачей

### Состояния таймера

- **stopped** - остановлен, показывает полное время
- **running** - работает, вычисляет оставшееся время на основе `start_time`
- **paused** - на паузе, сохраняет оставшееся время

### Вычисление времени

**Сервер хранит:**
- `start_time` - timestamp начала таймера
- `duration_seconds` - длительность в секундах
- `status` - текущий статус

**Клиент вычисляет:**
```javascript
const elapsed = Math.floor((Date.now() - start_time) / 1000);
const remaining = Math.max(0, duration_seconds - elapsed);
```

Это позволяет:
- Обновление страницы не сбрасывает таймер
- Точное отображение времени на клиенте
- Синхронизация между устройствами

## 🔄 Realtime синхронизация

### WebSocket архитектура

1. **Подключение:**
   - Клиент подключается к WebSocket серверу
   - Сервер проверяет cookie с `userId`
   - Пользователь добавляется в комнату

2. **Хранилище соединений:**
   ```javascript
   connections = { userId: WebSocket }
   rooms = { roomId: Set<userId> }
   ```

3. **События:**

   **От сервера к клиенту:**
   - `user:state` - обновление состояния пользователя (таймер + задача)
   - `pong` - ответ на heartbeat

   **От клиента к серверу:**
   - `ping` - heartbeat для поддержания соединения
   - `state:request` - запрос текущего состояния

4. **Уведомления:**
   - При изменении таймера → `notifyTimerUpdate(userId)`
   - При изменении задачи → `notifyTaskUpdate(userId)`
   - Состояние отправляется всем в комнате

### Пример потока данных

```
Пользователь A запускает таймер
  ↓
POST /api/timer/start
  ↓
Сервер обновляет БД
  ↓
notifyTimerUpdate(userA.id)
  ↓
WebSocket отправляет user:state всем в комнате
  ↓
Пользователь B получает обновление
  ↓
UI партнёра обновляется в реальном времени
```

## 🚀 Установка и запуск

### Требования

- Node.js 18+ (с поддержкой ES modules)
- npm или yarn

### Установка

```bash
# Установка зависимостей
npm install

# Запуск сервера
npm start

# Или в режиме разработки (с автоперезагрузкой)
npm run dev
```

Сервер запустится на `http://localhost:3000`

### Использование

1. Откройте `http://localhost:3000` в браузере
2. Введите invite-код и имя (например, "alice123" и "Алиса")
3. Создайте задачи и запустите таймер
4. Откройте в другом браузере/вкладке с тем же invite-кодом (например, "alice123" и "Боб")
5. Оба пользователя увидят таймеры и задачи друг друга в реальном времени

## 📡 API Endpoints

### Аутентификация

- `POST /api/auth/login` - Вход по invite-коду
- `GET /api/auth/me` - Получить текущего пользователя
- `POST /api/auth/logout` - Выход

### Задачи

- `GET /api/tasks` - Получить все задачи пользователя
- `POST /api/tasks` - Создать задачу
- `POST /api/tasks/:taskId/activate` - Активировать задачу
- `DELETE /api/tasks/:taskId` - Удалить задачу

### Таймер

- `GET /api/timer` - Получить текущее состояние таймера
- `POST /api/timer/start` - Запустить таймер
- `POST /api/timer/pause` - Приостановить таймер
- `POST /api/timer/resume` - Возобновить таймер
- `POST /api/timer/stop` - Остановить таймер

### Партнёр

- `GET /api/partner/state` - Получить состояние партнёра

## 🎯 Особенности реализации

### 1. Независимые таймеры

Каждый пользователь управляет только своим таймером. Партнёр видит состояние, но не может управлять.

### 2. Realtime обновления

WebSocket обеспечивает мгновенную синхронизацию без polling.

### 3. Устойчивость к перезагрузке

Состояние хранится на сервере. При обновлении страницы:
- Таймер продолжает работать (вычисляется на основе timestamp)
- Задачи сохраняются
- Активная задача остаётся активной

### 4. Простота входа

Нет регистрации, паролей, email. Только invite-код и имя.

### 5. Автоматическое создание комнат

Система автоматически создаёт комнаты и присоединяет пользователей.

## 🔧 Технические детали

### Зависимости

- **express** - HTTP сервер
- **ws** - WebSocket сервер
- **sqlite3** - База данных
- **uuid** - Генерация уникальных ID
- **cookie-parser** - Работа с cookies

### Безопасность

- httpOnly cookies для защиты от XSS
- Проверка прав доступа на сервере
- Валидация входных данных

### Производительность

- SQLite для простоты (можно заменить на PostgreSQL)
- WebSocket для эффективной realtime коммуникации
- Минимальные вычисления на клиенте

## 📝 Будущие улучшения

- [ ] Звуковые уведомления при завершении таймера
- [ ] Статистика работы (количество помидоров, время)
- [ ] Настройка длительности таймера на лету
- [ ] История задач
- [ ] Темная тема
- [ ] Мобильная адаптация

## 📄 Лицензия

MIT
